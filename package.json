{
  "name": "@honmameiko/apf-types",
  "version": "1.0.7",
  "description": "type declarations for both frontend and backend of the apf",
  "main": "./dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist/ && tsc",
    "publish": "npm run build && npm version patch && npm publish"
  },
  "keywords": [
    "Typescript"
  ],
  "author": "honmameiko",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.2.3"
  },
  "dependencies": {}
}
