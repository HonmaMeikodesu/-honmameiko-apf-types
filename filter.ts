export enum HasSolveFilterOption {有题解 = 0, 无题解 = 1}
export enum StatusFilterOption {未做 = 0, 尝试过 = 1, 已AC = 2}
export enum OriginFilterOption {自己 = 0, 别人 = 1}
export enum FilterKind {hasSolve, status, difficultyRange, origin}